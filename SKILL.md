---
name: lifeplan
description: マネーフォワードの家計データから、FIREシミュレーション付きライフプランシート（Google Sheets）を自動生成するスキル。リベシティ公式の支出管理表＋ライフプランシートをベースに、インフレ率・年代別生活費・大型ライフイベント・Die with Zero設計・想定寿命/退職年齢ドロップダウンまで全部組み込む。
trigger: 「ライフプランを作りたい」「FIREシミュレーションしたい」「家計データからライフプランを作って」「FIRE何歳でできるか計算して」「自分専用のライフプランを作って」などの依頼。または `/lifeplan` と入力したとき。
user_invocable: true
---

## 概要

リベシティ公式の「支出管理表＋ライフプランシート」を**自分専用に育てる**ためのスキル。マネーフォワードの実績データを取り込み、AIとの対話で前提条件を固め、Google Sheets にFIREシミュレーション付きの完成版を出力する。

**所要時間**: 60〜90分（ヒアリング込み）
**必要なもの**:
- マネーフォワード ME（無料プランで可）
- Googleアカウント
- リベシティ公式の支出管理表＋ライフプランシート（持っていない場合はリベシティから入手を案内）

**最終出力**: Google Sheets（8シート構成・**インフレ率・年代別生活費・想定寿命可変・退職年齢可変・最早FIRE可能年齢ファインダー**搭載）

---

## このスキルが目指すゴール

> **「想定寿命を80/85/90/95歳と切り替えるだけで、自分が何歳でFIREできるかが1歳刻みで瞬時に分かる」シートを完成させる。**

そのために以下の8つの機能を実装する：

1. **実績集計シート（新規）** — 複数年の実績を横断集計
2. **予算計画シートの費目細分化** — 自分の家計に合わせた支出費目
3. **マネーフォワード自動連携** — 毎月の実績を自動取り込み
4. **インフレ率の組み込み** — 年率2%を全期間に反映
5. **年代別生活費の変動** — 60代/70代/80代以降で段階的に変化
6. **大型ライフイベントの計上** — 教育費・介護費・予備費・車買い替え
7. **Die with Zero 設計** — 取り崩しを前提にFIRE年齢を計算
8. **FIRE資産の純化** — 子供名義口座など"使えない資産"を除外

---

## パイプライン全体像

```
Phase 1: ヒアリング              ← 現状・目標・ライフイベントを確認
Phase 2: マネーフォワードデータ取得  ← CSV取得 or 自動連携
Phase 3: データ分析               ← 費目別・年別に集計
Phase 4: FIRE条件の設計           ← AI対話で「自分のFIRE」を定義
Phase 5: テンプレートカスタマイズ   ← 8シート構成を生成
Phase 6: シミュレーション          ← 想定寿命×退職年齢の表
Phase 7: 仕上げと運用              ← 動作確認、運用ルール明文化
```

---

## Phase 1：ヒアリング

ユーザーに以下を順に質問する。**1問ずつ**質問することを徹底（一気に全部聞かない）。

### 必須項目

```
【基本情報】
1. 現在の年齢（本人）
2. 配偶者の有無、いる場合は年齢
3. 子供の人数と現在の年齢
4. 居住形態（持ち家/賃貸）
5. 持ち家の場合：住宅ローン残高・残年数

【収入】
6. 本人の手取り年収
7. 配偶者の手取り年収（あれば）
8. 不動産収入（あれば、年額）
9. 副業/事業収入（あれば、年額）
10. 配当収入（あれば、年額）

【現在の資産】
11. 金融資産合計（現金・預金・株・投信・iDeCo・NISA等）
12. うちFIRE資産から除外する分
    （例：子供名義口座、教育資金専用口座など）
13. 不動産評価額（あれば）

【FIRE目標】
14. FIRE後の想定生活費（年額）
15. 想定する寿命（80/85/90/95/100歳から選択 or 自由入力）
16. 期待する運用利回り（保守的3% / 標準4% / 標準的5% / 積極的6%）
17. FIREの形（完全FIRE / サイドFIRE / バリスタFIRE）

【ライフイベント】
18. 子供の進路想定（大学：国公立/私立/院進学）
19. 親の介護費用想定（保守的に4人分で見積もり推奨）
20. 車の買い替え予定（時期・金額）
21. 住宅リフォーム予定
22. その他大きな支出予定（旅行・趣味の道具など）
```

### ヒアリングの省略ルール

- 答えたくない項目は「●●●」で。その部分はレンジ計算で対応
- 最低限必要：年齢・年収・資産・FIRE目標年齢
- 全部一度に聞かない。**質問は1問ずつ**

### 重要な確認事項（ユーザーへの呼びかけ）

```
あなたのライフプランをより現実的にするため、以下の前提も確認させてください：

A. インフレ率は年率2%で計算します（過去30年の平均的な水準）。変えたい場合は教えてください。
B. 80歳以降の生活費は60代の70%まで段階的に減らします（一般的な高齢者の支出傾向）。
C. 親の介護費用は1人あたり500〜1,000万円で見積もります。両親4人分なら最大4,000万円。
D. 老後30年分の予備費として1,500万円（年50万円×30年）を別枠で計上します。
E. 退職金（早期退職含む）は一律1,800万円で想定します（実際の規定があればその金額）。
```

これらを変更したいかどうか、ユーザーに確認する。

---

## Phase 2：マネーフォワードデータ取得

### 方法A：CSV手動エクスポート（標準）

ユーザーへの案内：

```
マネーフォワード ME（PC版）から家計データをエクスポートしてください。

【手順】
1. https://moneyforward.com/ にログイン
2. 左メニュー「家計簿」→「収入・支出」
3. 期間を「過去2〜3年分」に設定（理想は3年、最低1年）
4. 右上「CSVダウンロード」
5. ダウンロードしたCSVのパスを教えてください

例：/Users/yourname/Downloads/収入・支出詳細_2024-01-01_2026-12-31.csv
```

### 方法B：自動連携（高度）

ユーザーが希望すれば、別途「mf-automation」スキル（または同等の仕組み）を使って毎月10日にCSV自動取得する設定を案内する。

### データが取得できない場合

- 通帳・給与明細から手入力
- 概算値で先にシート作成 → 後で実績値に差し替えるフロー

---

## Phase 3：データ分析

### 分析項目

```python
# CSV から自動計算する指標
analyses = {
    "年間収入（税引後）":   "収入カテゴリの合算",
    "年間支出（固定費）":   "住宅・保険・通信費・教育費等",
    "年間支出（変動費）":   "食費・娯楽・日用品等",
    "年間支出（特別）":     "旅行・大型家電・医療費等",
    "年間貯蓄額":           "収入 - 支出",
    "貯蓄率":               "貯蓄額 / 収入 × 100",
    "月平均生活費":         "支出 / 12",
    "費目別の年間支出":     "費目ごとに年別合計",
    "費目別の3年平均":      "実績集計シートに使う"
}
```

### 分析スクリプト

```bash
# CSV の文字コード確認と変換
file $CSV_PATH
nkf -w --overwrite $CSV_PATH 2>/dev/null || iconv -f SHIFT_JIS -t UTF-8 $CSV_PATH > /tmp/mf_utf8.csv

# Python で集計
python3 ~/.claude/skills/lifeplan/scripts/analyze_mf_csv.py /tmp/mf_utf8.csv
```

`analyze_mf_csv.py` が以下を出力する：
- 年別収支サマリー
- 費目別の年間集計（複数年）
- 異常値・欠損のフラグ

---

## Phase 4：FIRE条件の設計

### 設計の3ステップ

#### Step 4-1：必要生活費の算出

ヒアリングと実績データから、FIRE後の必要生活費を算出する。

```
基本：現在の月平均生活費 × 12 × 0.85〜0.95
（FIRE後は通勤費・服飾費が減るため85〜95%が一般的）

調整：
- 子供独立後 → さらに20%減
- 80歳以降 → さらに30%減（年代別調整）
```

#### Step 4-2：不労所得目標の算出

```
不労所得目標 = 必要生活費 - 既に確保している継続収入
            （年金・不動産収入・配当など）

例：
  必要生活費    年350万円
  老齢年金      年216万円（65歳以降）
  不動産収入    年100万円
  配当収入       年86万円
  ────────────────────
  足りない分     年−52万円 → 取り崩しが必要
```

#### Step 4-3：必要資産額の計算

```
基本式：必要資産 = 不足分 ÷ 運用利回り
例：250万円 ÷ 5% = 5,000万円

詳細式（Die with Zero対応）：
  必要資産 = (年間取り崩し額 − 年間追加収入) × 想定生存年数 ÷ 利回り係数

実際は Phase 5 で生成するシートが年単位で計算するので、
ここでは目安の数字として算出すればOK。
```

### ユーザーへの確認

```
以下の条件でシートを作成します。確認してください：

【FIRE条件】
- 想定寿命：●●歳
- FIRE後の必要生活費：年●●●万円
- うち継続収入で賄える分：年●●万円
- 資産収入で必要な額：年●●万円
- 必要資産額：●,●●●万円（年利●%想定）

【その他の前提】
- インフレ率：年率2%
- 80歳以降の生活費：60代の70%まで段階的減
- 介護費用枠：両親4人分で最大●,●●●万円
- 予備費：1,500万円（30年分）
- 退職金：1,800万円（退職時に一括）

修正があれば教えてください。OKなら次に進みます。
```

---

## Phase 5：テンプレートカスタマイズ

### 5-1：テンプレートの取得

```bash
TEMPLATE=~/.claude/skills/lifeplan/templates/lifeplan_template.xlsx
WORK_DIR=~/lifeplan_work
mkdir -p "$WORK_DIR"
cp "$TEMPLATE" "$WORK_DIR/my_lifeplan_$(date +%Y%m%d).xlsx"
```

このテンプレートには既に以下が組み込まれている：

- **8シート構成**（使い方/2025実績/2026実績/実績集計/予算計画/年間予算/ライフプラン/list）
- **想定寿命ドロップダウン**（F1セル、80/85/90/95から選択）
- **退職年齢ドロップダウン**（N1セル、50〜60から選択）
- **最早FIRE可能年齢ファインダー**（N2セル、自動計算）
- **教育費完了時の最低必要資産計算**（J2セル）
- **インフレ率2%の数式**（生活費に織り込み済み）
- **年代別生活費の変動**（60代/70代/80代以降で段階的減）
- **5%運用の累計資産計算**（行95、各年で複利計算）
- **退職金1,800万円**の組み込み（退職年齢に追従）

### 5-2：テンプレートの個人情報書き換え

ユーザーから聞いた以下の情報をテンプレートに書き込む：

```python
# scripts/customize_template.py が以下を実行
1. 家族の名前と現在年齢（C4-C7、D4-D7）
2. 開始年（D3 = 今年）
3. 予算計画シートの収入欄（給与・配偶者給与・不動産・配当）
4. 予算計画シートの支出費目（細分化された費目）
5. ライフイベント（教育費・車・介護・予備費・住宅リフォーム）
6. FIRE目標額（必要資産・運用利回り）
```

### 5-3：実行コマンド

```bash
python3 ~/.claude/skills/lifeplan/scripts/customize_template.py \
  --template "$WORK_DIR/my_lifeplan_$(date +%Y%m%d).xlsx" \
  --output "$WORK_DIR/my_lifeplan_customized.xlsx" \
  --config "$WORK_DIR/user_inputs.json"
```

`user_inputs.json` はヒアリングで集めた内容をJSON化したもの。

### 5-4：Google Sheetsへのアップロード

ユーザーへの案内：

```
カスタマイズ済みのxlsxを Google Sheets にアップロードしてください。

【手順】
1. https://drive.google.com/ を開く
2. xlsxファイルをドラッグ＆ドロップ
3. アップロード完了後、ファイル右クリック→「アプリで開く」→「Google スプレッドシート」
4. 上部メニュー「ファイル → Google スプレッドシートとして保存」で .gsheet 化
   （これをやらないと数式の一部が動きません）

これで完成です！
```

---

## Phase 6：シミュレーション

### 6-1：動作確認

ユーザーに以下を試してもらう：

```
ライフプランシートのタブを開いて、以下を確認してください：

1. 行1の「想定寿命」ドロップダウン（F1セル）
   → 80/85/90/95 を切り替えると、J1の「想定寿命到達時の累計資産」が変わるはず

2. 行2の「教育費完了時の最低必要資産」（J2セル）
   → これは「子供が独立した時にこの金額さえあればFIREが成立する」最低ライン

3. 行1の「退職年齢」ドロップダウン（N1セル）
   → 50〜60 を切り替えると、Q1がその退職年齢でのシナリオの想定寿命到達時資産

4. 行2の「最早FIRE可能年齢」（N2セル）
   → 資産が枯渇しない最も早い退職年齢が自動表示される
```

### 6-2：感度分析

```
以下を試して、どんな前提変更が最大のインパクトを生むか確認：

A. 想定寿命：80 → 95 で必要資産がどう変わるか
B. 退職年齢：60 → 55 で最早FIREの達成可能性
C. 運用利回り：4% → 5% で必要資産が約25%減
D. 80歳以降生活費：70% → 100% でどう変わるか
```

---

## Phase 7：仕上げと運用

### 7-1：運用ルールの明文化

シート最初の「📖 使い方・運用ルール」シートに以下を記入：

```
【運用方針】
日々の実績は自動で記録、ライフプランは年1回だけ手動で見直す。

【自動化されている部分】
- マネーフォワードから毎月10日にCSV自動取り込み
- 実績集計シートで自動的に複数年平均を計算
- ライフプランシートの累計資産が自動更新

【手動で見直す部分（年1回）】
- 1月：去年の実績を実績集計シートに反映
- 1月：今年の予算計画シートを更新
- 1月：ライフプランシートの想定寿命/退職年齢を再検討
- 必要に応じて：大型ライフイベントの追加・修正

【見直しの判断材料】
- N2セル「最早FIRE可能年齢」の変化
- J1セル「想定寿命到達時の累計資産」の変化
- 実績との乖離が大きい費目
```

### 7-2：可視化（オプション）

```bash
# 資産推移グラフをHTML→PNG化
python3 ~/.claude/skills/lifeplan/scripts/generate_charts.py \
  --xlsx "$WORK_DIR/my_lifeplan_customized.xlsx" \
  --out-dir "$WORK_DIR/charts/"
```

出力されるグラフ：
- 累計資産の年別推移
- 退職年齢別のシミュレーション結果
- インフレ込み vs インフレなしの比較

---

## カスタマイズの肝（記事で約束した8項目の実装方法）

### ① インフレ率の組み込み

```
予算計画シートで設定した費目の年間金額に、年ごとに 1.02^(年-基準年) を掛ける。
ライフプランシートの数式：
  =予算計画シート!B$X * POWER(1.02, ライフプランシート!Y3 - 基準年)
```

### ② 年代別生活費の変動

```
予算計画シートの「変動費」費目に年齢補正を入れる：
  60代  → 100%
  70代  → 90%
  80代以降 → 70%

数式例：
  =変動費基本額 * IF(年齢<70, 1, IF(年齢<80, 0.9, 0.7))
```

### ③ 大型ライフイベントの計上

予算計画シートの「特別な支出」セクション（行76付近）に以下を年単位で入力：

| 費目 | タイミング | 金額 |
|------|----------|------|
| 教育費（子1大学） | 入学年〜卒業年 | 年300万円 × 4年 |
| 教育費（子2大学） | 同上 | 年300万円 × 4年 |
| 親の介護費用 | 60〜90歳に分散 | 計4,000万円 |
| 予備費 | 60〜90歳に均等 | 年50万円 × 30年 |
| 車買い替え | 5〜7年ごと | 200万円 × 数回 |
| 住宅リフォーム | 退職前後 | 500万円 |

### ④ Die with Zero 設計

```
ライフプランシート 行95（累計資産）の式：
  =前年資産 * 1.05 + 当年収入 - 当年支出

「ゼロで着地」を目指す設計：
  想定寿命時の累計資産が「目標バッファ」と等しくなるように、
  退職年齢・生活費を調整する。
```

### ⑤ 退職金（一律1,800万円）

```
予算計画シートの収入欄に「退職金」費目を追加：
  退職する年に1,800万円を一括計上。

退職年齢ドロップダウン（N1）に連動させる場合：
  =IF(年齢=退職年齢, 18000000, 0)
```

### ⑥ 想定寿命ドロップダウン

```
ライフプランシート F1 セル：
- データの入力規則：リスト = 80,85,90,95
- 既定値：95

J1 セル（想定寿命到達時の累計資産）：
=IFERROR(INDEX(D95:BA95, MATCH($F$1, D4:BA4, 0)), "範囲外")
```

### ⑦ 退職年齢ドロップダウンと最早FIRE可能年齢

```
ライフプランシート N1 セル：
- データの入力規則：リスト = 50,51,52,...,60
- 既定値：60

Q1 セル（退職年齢適用後の想定寿命時資産）：
=INDEX(D95:BA95, MATCH($F$1, D4:BA4, 0))
 - SUMPRODUCT((D4:BA4>$N$1)*(D4:BA4<=60)*(D25:BA25+D28:BA28)*POWER(1.05, $F$1 - IFERROR(D4:BA4, $F$1)))
 + 18000000 * POWER(1.05, $F$1 - $N$1)
 - 18000000 * POWER(1.05, $F$1 - 60)

ヘルパー行（97〜98）に退職年齢50〜60の各シナリオを並列計算し、
N2 セルで「最早FIRE可能年齢」を抽出。
```

### ⑧ FIRE資産の純化

```
予算計画シートの「現在の資産」入力欄で、子供名義口座など
"使えない資産"を除外した純粋な FIRE 用資産額を入力する。
```

---

## よくある質問・対処法

### Q: マネーフォワードのCSVを読み込めない
```
→ 文字コードの問題が多い：
   nkf -w --overwrite ファイル名.csv
   または iconv -f SHIFT_JIS -t UTF-8 ファイル名.csv > 変換後.csv
```

### Q: 収入が不安定でシミュレーションが難しい
```
→ 過去3年の平均値を使う
→ 楽観・標準・保守の3シナリオで計算する
```

### Q: 不動産収入の計算
```
→ 不動産収入 - 経費（修繕・管理・税金）= 実質手取り
→ 入居率低下リスク：実質収入 × 0.8 推奨
```

### Q: 退職金が会社規定で1,800万円じゃない
```
→ 予算計画シート行28の金額を実際の額に変更
→ 早期退職割増がある場合は、退職年齢ごとの金額も設定可能
```

---

## 判断ルール

- ヒアリングは**1問ずつ**。一気に全部聞かない
- 答えたくない項目はマスキングしてレンジ計算で対応
- 数値が1つでも揃えばシート作成を開始する（後から更新可能）
- 完璧主義にならず「まず動くシートを作る」を優先
- 税金（住民税・所得税）の計算は概算でOK（精密計算はアキラが担当）

---

## 関連スキル・エージェント

- **ユウコ**: 家計の節約・運用アドバイス（このスキルの本拠地）
- **アキラ**: FIRE時の税金・社会保険料の精密計算
- **タツヤ**: 不動産収入を含めたFIRE設計
- **libecity-article**: このスキルで作ったライフプランをブログ記事化

---

## 関連ファイル

```
~/.claude/skills/lifeplan/
├── SKILL.md                                    ← このファイル
├── README.md                                   ← 開発者向けメモ
├── templates/
│   └── lifeplan_template.xlsx                  ← 8シート構成のテンプレート
├── scripts/
│   ├── analyze_mf_csv.py                       ← MF CSV分析
│   ├── customize_template.py                   ← テンプレートカスタマイズ
│   └── generate_charts.py                      ← グラフ生成
└── docs/
    └── customization_guide.md                  ← 数式の技術詳細
```

---

## 前提条件

- マネーフォワード ME（無料プランでも月12件まで閲覧可能）
- Googleアカウント（Google Sheets用）
- Python 3（Claude Code 実行環境にあれば動く）
- 個人情報の取り扱い：すべてローカル処理・外部送信なし
